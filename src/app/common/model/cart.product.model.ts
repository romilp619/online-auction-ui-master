export interface CartProduct {
  id: number;
  name: string;
  description: string;
  categoryName: string;
  productPrice: number;
  totalPrice: number;
  quantity: string;
  firstImageData: string;
}
